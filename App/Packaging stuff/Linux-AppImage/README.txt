Inside OpenLDAT.AppDir:
* Place an x86_64 version of the JRE from OpenJDK inside the jre folder
* Copy the binaries from the dist folder of the OpenLDAT project inside the openldat folder
* Run `appimagetool OpenLDAT.AppDir`
